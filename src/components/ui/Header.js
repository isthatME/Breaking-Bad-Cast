import React from 'react'
import Logo from '../../assets/logo.png'

const Header = () => {
    return (
        <header>
            <img src={Logo} alt=""/>
        </header>
    )
}

export default Header
